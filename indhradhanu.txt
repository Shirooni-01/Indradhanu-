THE PROJECT C :
WE WILL NEED AN THERMAL CAMERA 


Today we gonna work on new project named as Project C or indradhanu, the project is simple that is : we know now a days the wildness of wild animals in incresing due to deforestration and human interferance in their homeland, and it becoming big problem coz the animal are now killing other animal and even humans. so we gonna design a model (hardware + software) which will detech the wild animals which are : Lion, Tiger, Bear(indian) and Lepords. after detection of animal the alert will be sent to nearby villager and the authority of that village using sms. Hardware which we gonna use for this project is rasberry pi 4, thermal camera, an rotator to rotate camera, etc. the bottleneck cases of this projects will be if their is no internet for that we gonna use Sqlite for fallback if their is no network the fall back system will work and when the system is in network then the process will br processed. The tech stack will be like this (can add somethign afterward but this is fix): HTML, CSS, JS, python (Flask), Python, etc. The requriment for this project that i think is needed is a ML model which will detect the animals. a live map, and something. This is complete idea which we gonna work on. 
